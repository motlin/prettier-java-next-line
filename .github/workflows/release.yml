name: 🚀 Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release type (patch, minor, major, prepatch, preminor, premajor, prerelease)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
          - prepatch
          - preminor
          - premajor
          - prerelease
      preid:
        description: 'Prerelease identifier (alpha, beta, rc, etc.)'
        required: false
        default: ''
  push:
    tags:
      - 'v*'
      - 'maculate-java@*'

jobs:
  release:
    name: 📦 Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      id-token: write
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔧 Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.mise.toml'
          registry-url: 'https://registry.npmjs.org'
          cache: false

      - name: 📚 Install dependencies
        run: yarn install --frozen-lockfile

      - name: 🏗️ Build packages
        run: yarn build

      - name: 🧪 Run tests
        run: yarn test

      - name: 🔍 Run linting
        run: yarn lint

      - name: 📋 Check formatting
        run: yarn format:validate

      - name: 🔖 Version packages (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [ -n "$RELEASE_PREID" ]; then
            yarn lerna:version "$RELEASE_VERSION" --preid "$RELEASE_PREID" --yes
          else
            yarn lerna:version "$RELEASE_VERSION" --yes
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_VERSION: ${{ github.event.inputs.version }}
          RELEASE_PREID: ${{ github.event.inputs.preid }}

      - name: 📢 Publish to npm
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" >> ~/.npmrc
          yarn lerna:publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: 🏷️ Get version info
        id: version
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          MACULATE_VERSION=$(node -p "require('./package.json').version")
          echo "maculate_version=$MACULATE_VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$REF_NAME" >> "$GITHUB_OUTPUT"

      - name: 📝 Generate release notes
        id: release_notes
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
          MACULATE_VERSION: ${{ steps.version.outputs.maculate_version }}
          REPOSITORY: ${{ github.repository }}
        run: |
          cat << EOF > release_notes.md
          ## 🎉 Release ${RELEASE_TAG}

          ### 📦 Package Versions
          - **maculate-java**: v${MACULATE_VERSION}

          ### 📥 Installation

          \`\`\`bash
          npm install --save-dev maculate-java@${MACULATE_VERSION}
          \`\`\`

          or with Yarn:

          \`\`\`bash
          yarn add --dev maculate-java@${MACULATE_VERSION}
          \`\`\`

          ### 🔄 What's Changed

          Please see the [CHANGELOG](https://github.com/${REPOSITORY}/blob/main/CHANGELOG.md) for detailed changes.

          ---

          **Full Changelog**: https://github.com/${REPOSITORY}/compare/$(git describe --tags --abbrev=0 HEAD^)...${RELEASE_TAG}
          EOF

      - name: 🎉 Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_notes.md
          draft: false
          prerelease: ${{ contains(steps.version.outputs.tag, 'alpha') || contains(steps.version.outputs.tag, 'beta') || contains(steps.version.outputs.tag, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}