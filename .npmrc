# Ensure package-lock is not created (using yarn)
package-lock=false

# Registry configuration
registry=https://registry.npmjs.org/

# Access level for scoped packages (if we decide to use @scope/maculate-java later)
access=public

# Save exact versions
save-exact=true